class Note {
    constructor(items) {
        this.id = items['id_note'];
        this.title = items['title'];
        this.discription = items['discription'];
        this.date = items['date'];
    }
}